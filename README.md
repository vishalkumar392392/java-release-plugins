# java-release-plugins

mvn -B release:clean
mvn -B release:prepare
mvn -B release:perform

To run the jfrog artifactory in your local go to the artifactory path and run below command
artifactory.bat
